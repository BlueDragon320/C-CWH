#include <stdio.h>
int main(){

    int n , b;
    printf("Enter a number: ");
    scanf("%d", &n);
    for (int i = 10; i ; i--)
    {
        printf("%d X %d = %d\n", n, i, n*i );
    }
    
    return 0;
}

// q3) a
// q4) true
